/* tslint:disable:no-unused-variable */

import { DatePipePipe } from './date-pipe.pipe';

describe('Pipe: DatePipee', () => {
  it('create an instance', () => {
    let pipe = new DatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
